#include <stdio.h>
#include <stdlib.h>

#include "clientes.h"

typedef struct node* link;

struct node{
	Cli cliente;
	link next;
};

typedef struct LISTA{

}lista;

novalista, insereordenado, pesquisa, eliminacliente, limpalista

